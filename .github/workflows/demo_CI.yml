name: Hello World

on: workflow_dispatch

jobs:
  Build:
    runs-on: ubuntu-latest

    steps:
      - name: Hello Step
        run: echo "Hello ${{vars.MYNAME}} This is my 1st Build"
  Deployment:
    runs-on: ubuntu-latest
    needs: Build

    steps:
      - name: Hello Deployment
        run: echo "Hello ${{vars.MYNAME}}This is my 1st deployment"
